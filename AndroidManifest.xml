<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.kyriog.android.voxpopuli"
    android:versionCode="2014022201"
    android:versionName="0.2α" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="fr.kyriog.android.voxpopuli.HomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="fr.kyriog.android.voxpopuli.LoginActivity"
            android:label="@string/title_activity_login"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name="fr.kyriog.android.voxpopuli.GameActivity">
        </activity>
    </application>

</manifest>
